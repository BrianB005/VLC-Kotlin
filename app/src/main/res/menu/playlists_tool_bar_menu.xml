<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    tools:ignore="HardCodedText">

    <item
        android:icon="@drawable/ic_baseline_search_24"
        android:id="@+id/search_playlists"
        android:title="Search playlists"

        app:showAsAction="ifRoom"
        />
    <item
        android:icon="@drawable/ic_baseline_sort_24"
        android:id="@+id/sort_by"
        android:title="Sort by"

        app:showAsAction="ifRoom"
        >
        <menu>
            <item android:title="Name"
                android:id="@+id/sort_by_name_descending"
                android:icon="@android:drawable/arrow_down_float"
                />
            <item android:title="Name"
                android:id="@+id/sort_by_name_ascending"
                android:icon="@android:drawable/arrow_up_float"
                android:visible="false"
                />
        </menu>
    </item>
    <item android:title="Display in list"
        android:id="@+id/display_in_list"
        />
    <item android:title="Refresh"
        android:id="@+id/refresh_browse"
        />

</menu>